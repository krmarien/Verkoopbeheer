{% extends 'common/layout.twig' %}

{% block content %}
    <div class="page-header">
        <h1>Item Toevoegen <small>een stock item toevoegen</small></h1>
    </div>

    <div class="row">
        <div class="span3 columns">
            <p>Hier kan je een stock item toevoegen aan Verkoopbeheer.</p>
            <a href="{{ url('common_stock') }}" class="btn">Terug</a>
        </div>
        <div class="span9 columns">
            {% import 'common/partials/form.twig' as forms %}
            {{ forms.renderForm(form) }}
        </div>
    </div>
{% endblock %}