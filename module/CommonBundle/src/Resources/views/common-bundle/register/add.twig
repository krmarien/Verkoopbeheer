{% extends 'common/layout.twig' %}

{% block content %}
    <div class="page-header">
        <h1>Kassa Toevoegen <small>een kassa toevoegen aan telling <em>{{ counting.getDescription() }}</em></small></h1>
    </div>

    <div class="row">
        <div class="span3 columns">
            <p>Hier kan je een telling toevoegen aan Verkoopbeheer.</p>
            <a href="{{ url('common_counting', {'action': 'view', 'id': counting.getId()}) }}" class="btn">Terug</a>
        </div>
        <div class="span9 columns">
            {% import 'common/partials/form.twig' as forms %}
            {{ forms.renderForm(form) }}
        </div>
    </div>
{% endblock %}